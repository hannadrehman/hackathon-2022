{
  "name": "hackyard-vr",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",
    "@react-three/drei": "9.17.1",
    "@react-three/fiber": "8.2.0",
    "@react-three/postprocessing": "2.4.6",
    "@types/three": "0.142.0",
    "leva": "0.9.29",
    "three": "0.142.0",
    "gh-pages" : "*"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf ./build&&react-scripts build && yarn mv",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "__predeploy": "npm run build",
    "__deploy": "gh-pages -b gh-pages -d build",
    "mv": "rm -rf ./docs && cp -R ./build ./docs && rm -rf ./build"
  },
  "homepage": ".",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
